/** Giorni oltre i quali una lavorazione aperta genera alert «in ritardo». */
export const KPI_OPEN_LATE_DAYS_THRESHOLD = 14;

/** Finestra (giorni) per recidiva operativa: ≥2 chiusure stesso mezzo. */
export const KPI_RECIDIVA_WINDOW_DAYS = 30;

/** Top N per classifiche economiche / flotta. */
export const KPI_TOP_N = 5;
