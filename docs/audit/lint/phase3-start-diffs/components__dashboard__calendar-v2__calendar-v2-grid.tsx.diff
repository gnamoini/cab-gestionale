diff --git a/components/dashboard/calendar-v2/calendar-v2-grid.tsx b/components/dashboard/calendar-v2/calendar-v2-grid.tsx
index c70d7d04..27f47a13 100644
--- a/components/dashboard/calendar-v2/calendar-v2-grid.tsx
+++ b/components/dashboard/calendar-v2/calendar-v2-grid.tsx
@@ -52,6 +52,7 @@ export function CalendarV2Grid({
   onGoToday?: () => void;
 }) {
   const [mounted, setMounted] = useState(false);
+  // eslint-disable-next-line react-hooks/set-state-in-effect -- client-only after mount
   useEffect(() => setMounted(true), []);
 
   const [yearStr, monthStr] = monthKey.split("-");
@@ -82,7 +83,7 @@ export function CalendarV2Grid({
 
   return (
     <div className={`${globalInputCalendarGridShell} min-w-0`}>
-      <div className="mb-2 flex min-w-0 flex-wrap items-center justify-between gap-2 px-1">
+      <div className="mb-2 flex min-w-0 items-center justify-between gap-2 px-1 flex-nowrap sm:flex-wrap">
         <div className="flex min-w-0 items-center gap-1">
           <button
             type="button"
