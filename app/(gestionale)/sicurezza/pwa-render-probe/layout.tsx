export { sicurezzaPwaRenderProbePageMetadata as metadata } from "@/lib/site/app-page-metadata";

export default function PwaRenderProbeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
