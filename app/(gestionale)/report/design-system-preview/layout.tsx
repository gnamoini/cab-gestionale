import type { ReactNode } from "react";

export { reportDesignSystemPreviewPageMetadata as metadata } from "@/lib/site/app-page-metadata";

export default function ReportDesignSystemPreviewLayout({ children }: { children: ReactNode }) {
  return children;
}
