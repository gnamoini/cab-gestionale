"use client";

/** Panel associazione entità — tier conservativo (Fase 2 UI shell). */
export function CaptureEntityLinkPanel() {
  return (
    <p className="text-sm text-[color:var(--cab-muted-fg)]">
      Associazione mezzo/lavorazione con tier conservativo — configurabile dopo estrazione campi.
    </p>
  );
}
