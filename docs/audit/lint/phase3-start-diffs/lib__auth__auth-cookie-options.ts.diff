diff --git a/lib/auth/auth-cookie-options.ts b/lib/auth/auth-cookie-options.ts
index e9012bae..90fc739d 100644
--- a/lib/auth/auth-cookie-options.ts
+++ b/lib/auth/auth-cookie-options.ts
@@ -23,7 +23,7 @@ export function resolveAuthCookieOptions(
   if (options.maxAge === 0) return options;
 
   if (!remember) {
-    const { maxAge: _maxAge, expires: _expires, ...sessionOptions } = options;
+    const { ...sessionOptions } = options;
     return sessionOptions;
   }
 
