diff --git a/src/lib/auth/auth-session-coordinator.client.ts b/src/lib/auth/auth-session-coordinator.client.ts
index 737cb783..9e32d917 100644
--- a/src/lib/auth/auth-session-coordinator.client.ts
+++ b/src/lib/auth/auth-session-coordinator.client.ts
@@ -93,7 +93,7 @@ async function runReconcile(sb: SupabaseClient): Promise<ReconcileResult> {
 /** Riconciliazione sessione pura — single-flight in-flight, nessun side effect. */
 export function reconcileSession(
   sb: SupabaseClient,
-  _opts: { reason: ReconcileReason; force?: boolean },
+  
 ): Promise<ReconcileResult> {
   if (inFlightPromise) {
     return inFlightPromise;
