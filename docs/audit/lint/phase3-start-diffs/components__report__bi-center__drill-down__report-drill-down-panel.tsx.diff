diff --git a/components/report/bi-center/drill-down/report-drill-down-panel.tsx b/components/report/bi-center/drill-down/report-drill-down-panel.tsx
index 8b695afd..b89c8efe 100644
--- a/components/report/bi-center/drill-down/report-drill-down-panel.tsx
+++ b/components/report/bi-center/drill-down/report-drill-down-panel.tsx
@@ -61,7 +61,7 @@ export function ReportDrillDownPanel({
   const query = useReportDrilldownQuery(request, open && context != null);
   const [recordTarget, setRecordTarget] = useState<ReportDrillDownRecordTarget | null>(null);
 
-  const pages = query.data?.pages ?? [];
+  const pages = useMemo(() => query.data?.pages ?? [], [query.data?.pages]);
   const first = pages[0];
   const rows = useMemo(
     () => pages.flatMap((p) => p.page?.rows ?? []),
@@ -103,7 +103,7 @@ export function ReportDrillDownPanel({
         restoreFocusRef={restoreFocusRef}
         ariaLabel="Pannello drill-down report"
       >
-        <div className="flex min-h-0 flex-1 flex-col overflow-hidden" data-testid="report-drilldown-panel">
+        <div className="flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden" data-testid="report-drilldown-panel">
           {query.isLoading ? (
             <div className="space-y-2 overflow-y-auto p-4" aria-busy="true">
               {Array.from({ length: 5 }).map((_, i) => (
@@ -123,7 +123,7 @@ export function ReportDrillDownPanel({
               <div className="shrink-0 border-b border-[color:var(--cab-border)] px-4 pt-4">
                 <ReportDrillDownHeaderView header={first.header} />
               </div>
-              <div className="gestionale-scrollbar min-h-0 flex-1 overflow-y-auto px-4 py-4">
+              <div className="gestionale-scrollbar min-h-0 min-w-0 flex-1 overflow-y-auto px-4 py-4">
                 {first.drillDownKind === "composition_analysis" && first.composition ? (
                   <ReportDrillDownCompositionView components={first.composition} />
                 ) : (
