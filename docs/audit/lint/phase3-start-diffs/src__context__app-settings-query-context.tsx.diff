diff --git a/src/context/app-settings-query-context.tsx b/src/context/app-settings-query-context.tsx
index 97af6e8b..f468ab9c 100644
--- a/src/context/app-settings-query-context.tsx
+++ b/src/context/app-settings-query-context.tsx
@@ -14,27 +14,7 @@ const AppSettingsQueryContext = createContext<UseQueryResult<CabAppSettingsQuery
 /** Owner unico fetch settings STATIC — montare una volta sotto QueryProvider. */
 export function AppSettingsQueryProvider({ children }: { children: ReactNode }) {
   const q = useCabAppSettingsPayloadQuery({ tier: "static", owner: true });
-  const value = useMemo(
-    () => q,
-    [
-      q.data,
-      q.dataUpdatedAt,
-      q.error,
-      q.errorUpdatedAt,
-      q.failureCount,
-      q.failureReason,
-      q.fetchStatus,
-      q.isError,
-      q.isFetched,
-      q.isFetching,
-      q.isLoading,
-      q.isPending,
-      q.isSuccess,
-      q.status,
-      q.refetch,
-      q.isRefetching,
-    ],
-  );
+  const value = useMemo(() => q, [q]);
   return <AppSettingsQueryContext.Provider value={value}>{children}</AppSettingsQueryContext.Provider>;
 }
 
