diff --git a/components/workshop-schedule/agenda-session-detail-panel.tsx b/components/workshop-schedule/agenda-session-detail-panel.tsx
index ec2989f1..746fae8c 100644
--- a/components/workshop-schedule/agenda-session-detail-panel.tsx
+++ b/components/workshop-schedule/agenda-session-detail-panel.tsx
@@ -25,13 +25,13 @@ export function AgendaSessionDetailPanel({
 }) {
   return (
     <div className={`${dsSurfacePanelStatic} min-h-0 gap-3 p-4`}>
-      <div className="flex min-w-0 flex-wrap items-start justify-between gap-3">
+      <div className="flex min-w-0 items-start justify-between gap-3 flex-nowrap sm:flex-wrap">
         <div className="min-w-0 flex-1">
           <p className={dsSectionTitle}>{session.title}</p>
           {session.description ? (
             <p className={`mt-1 ${dsTypoCaption}`}>{session.description}</p>
           ) : null}
-          <div className="mt-2 flex min-w-0 flex-wrap gap-2">
+          <div className="mt-2 flex min-w-0 gap-2 flex-nowrap sm:flex-wrap">
             <span className={PLANNING_STATUS_BADGE_CLASS[session.planningStatus]}>
               {PLANNING_STATUS_LABELS[session.planningStatus]}
             </span>
@@ -45,7 +45,7 @@ export function AgendaSessionDetailPanel({
             ) : null}
           </div>
         </div>
-        <div className="flex min-w-0 flex-wrap gap-2">
+        <div className="flex min-w-0 gap-2 flex-nowrap sm:flex-wrap">
           {session.workOrderId ? (
             <Tooltip content="Apri la lavorazione collegata">
               <Link
