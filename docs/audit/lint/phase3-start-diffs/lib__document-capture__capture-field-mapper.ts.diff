diff --git a/lib/document-capture/capture-field-mapper.ts b/lib/document-capture/capture-field-mapper.ts
index a2fba457..bb7d3aff 100644
--- a/lib/document-capture/capture-field-mapper.ts
+++ b/lib/document-capture/capture-field-mapper.ts
@@ -285,7 +285,7 @@ export function resolveCaptureRicambioRowText(
   descrizioneOcr: string,
   addettiRecords?: readonly AddettoRecord[],
 ): { ricambioNome: string; addetto: string } {
-  let nome = nomeOcr.trim();
+  const nome = nomeOcr.trim();
   let desc = descrizioneOcr.trim();
 
   if (!desc && nome) {
