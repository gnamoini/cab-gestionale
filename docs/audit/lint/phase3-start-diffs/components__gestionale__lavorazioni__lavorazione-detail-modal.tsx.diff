diff --git a/components/gestionale/lavorazioni/lavorazione-detail-modal.tsx b/components/gestionale/lavorazioni/lavorazione-detail-modal.tsx
index f43d1e06..f445d07b 100644
--- a/components/gestionale/lavorazioni/lavorazione-detail-modal.tsx
+++ b/components/gestionale/lavorazioni/lavorazione-detail-modal.tsx
@@ -105,6 +105,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
   );
 
   useEffect(() => {
+    // eslint-disable-next-line react-hooks/set-state-in-effect -- sync state in effect lifecycle
     setTab("panoramica");
   }, [lavorazioneId]);
 
@@ -132,6 +133,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
   useEffect(() => {
     resetScPage();
   }, [lavorazioneId, hub?.schede.length, listPageSize, resetScPage]);
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   const pagedSchede = useMemo(() => sliceSc(hub?.schede ?? []), [hub?.schede, sliceSc, scPage]);
 
   const {
@@ -146,6 +148,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
   useEffect(() => {
     resetMovPage();
   }, [lavorazioneId, hub?.movimenti.length, listPageSize, resetMovPage]);
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   const pagedMov = useMemo(() => sliceMov(hub?.movimenti ?? []), [hub?.movimenti, sliceMov, movPage]);
 
   const {
@@ -160,6 +163,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
   useEffect(() => {
     resetPvPage();
   }, [lavorazioneId, preventiviUi.length, listPageSize, resetPvPage]);
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   const pagedPv = useMemo(() => slicePv(preventiviUi), [preventiviUi, slicePv, pvPage]);
 
   const {
@@ -174,6 +178,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
   useEffect(() => {
     resetDocPage();
   }, [lavorazioneId, documentiUi.length, listPageSize, resetDocPage]);
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   const pagedDoc = useMemo(() => sliceDoc(documentiUi), [documentiUi, sliceDoc, docPage]);
 
   const tabBtn = (id: TabId, label: string) => {
@@ -217,7 +222,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
           </div>
         ) : null}
 
-        <div className="flex shrink-0 flex-wrap gap-1.5 border-b border-zinc-100 bg-zinc-50/50 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-900/80">
+        <div className="flex shrink-0 gap-1.5 border-b border-zinc-100 bg-zinc-50/50 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-900/80 min-w-0 flex-nowrap sm:flex-wrap">
           {tabBtn("panoramica", "Panoramica")}
           {tabBtn("schede", `Schede (${hub?.kpi.countSchede ?? 0})`)}
           {tabBtn("movimenti", `Movimenti (${hub?.kpi.countMovimenti ?? 0})`)}
@@ -264,7 +269,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
                 <p className="mt-1 whitespace-pre-wrap text-sm text-zinc-800 dark:text-zinc-200">{(hub.lavorazione.note ?? "").trim() || "—"}</p>
               </div>
               <LavorazionePlanningPanel lavorazioneId={lavorazioneId} />
-              <div className="flex flex-wrap gap-2">
+              <div className="flex gap-2 min-w-0 flex-nowrap sm:flex-wrap">
                 <Link
                   href={buildPreventiviArchivioFilterHref(
                     hub.lavorazioneId,
@@ -364,7 +369,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
                   pagedPv.map((p) => (
                     <li
                       key={p.id}
-                      className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-zinc-100 bg-zinc-50/60 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-800/40"
+                      className="flex items-center justify-between gap-2 rounded-lg border border-zinc-100 bg-zinc-50/60 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-800/40 min-w-0 flex-nowrap sm:flex-wrap"
                     >
                       <div className="min-w-0">
                         <p className="font-semibold text-zinc-900 dark:text-zinc-50">
@@ -372,7 +377,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
                         </p>
                         <p className="text-xs text-zinc-500">{p.cliente}</p>
                       </div>
-                      <div className="flex shrink-0 flex-wrap gap-1">
+                      <div className="flex shrink-0 gap-1 min-w-0 flex-nowrap sm:flex-wrap">
                         <button
                           type="button"
                           className={erpBtnNeutral}
@@ -412,7 +417,7 @@ export function LavorazioneDetailModal({ lavorazioneId, onClose }: { lavorazione
                     return (
                       <li
                         key={d.id}
-                        className="flex flex-wrap items-center justify-between gap-2 rounded-lg border border-zinc-100 bg-zinc-50/60 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-800/40"
+                        className="flex items-center justify-between gap-2 rounded-lg border border-zinc-100 bg-zinc-50/60 px-3 py-2 dark:border-zinc-800 dark:bg-zinc-800/40 min-w-0 flex-nowrap sm:flex-wrap"
                       >
                         <div className="min-w-0">
                           <p className="text-[11px] font-semibold uppercase text-zinc-500">{formatDocumentoRigaSintetica(d)}</p>
