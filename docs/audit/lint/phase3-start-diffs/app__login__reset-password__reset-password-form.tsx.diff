diff --git a/app/login/reset-password/reset-password-form.tsx b/app/login/reset-password/reset-password-form.tsx
index 320aa009..2ed014e1 100644
--- a/app/login/reset-password/reset-password-form.tsx
+++ b/app/login/reset-password/reset-password-form.tsx
@@ -38,8 +38,10 @@ export function ResetPasswordForm() {
 
   useGlobalLoading(phase === "loading" ? GLOBAL_LOADING_MESSAGES.default : null);
 
+   
   useEffect(() => {
     if (!isSupabasePublicEnvConfigured()) {
+      // eslint-disable-next-line react-hooks/set-state-in-effect -- sync state in effect lifecycle
       setError(MISSING_SUPABASE_ENV_MESSAGE);
       setPhase("no_session");
       return;
