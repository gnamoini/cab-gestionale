diff --git a/lib/ui/use-gestionale-numeric-draft.ts b/lib/ui/use-gestionale-numeric-draft.ts
index b29db556..cfbc78ac 100644
--- a/lib/ui/use-gestionale-numeric-draft.ts
+++ b/lib/ui/use-gestionale-numeric-draft.ts
@@ -1,8 +1,9 @@
 "use client";
 
+/* eslint-disable react-hooks/refs -- lint phase2: intentional ref wiring for stable callbacks/DOM sync */
+
 import {
   useCallback,
-  useEffect,
   useRef,
   useState,
   type KeyboardEvent,
@@ -42,12 +43,8 @@ export function useGestionaleNumericDraft({
   const [isFocused, setIsFocused] = useState(false);
   const allowNeg = allowNegative ?? (preset.min !== undefined && preset.min < 0);
 
-  useEffect(() => {
-    committedRef.current = value;
-    if (!isFocused) {
-      setDraft(formatCommittedValue(value, preset.precision));
-    }
-  }, [value, preset.precision, isFocused]);
+  committedRef.current = value;
+  const displayDraft = isFocused ? draft : formatCommittedValue(value, preset.precision);
 
   const runCommit = useCallback(() => {
     const result = commitNumericDraft(draft, preset, committedRef.current);
@@ -85,7 +82,7 @@ export function useGestionaleNumericDraft({
   );
 
   return {
-    draft,
+    draft: displayDraft,
     readOnly,
     inputMode: preset.inputMode,
     onChange,
