diff --git a/app/api/inventory-labels/bulk/route.ts b/app/api/inventory-labels/bulk/route.ts
index bcd0eb7d..5d998946 100644
--- a/app/api/inventory-labels/bulk/route.ts
+++ b/app/api/inventory-labels/bulk/route.ts
@@ -40,7 +40,7 @@ export async function POST(request: Request) {
   }
 
   const normalized = normalizeBulkLabelRequest(parsed.data);
-  const { items, preset, includeBarcode, clienteLabel, totalLabels } = normalized;
+  const { items, preset, clienteLabel, totalLabels } = normalized;
   const origin = requestOrigin(request);
 
   if (isBulkSyncCount(totalLabels)) {
@@ -49,7 +49,6 @@ export async function POST(request: Request) {
         await renderBulkLabelPdfSync({
           items,
           preset,
-          includeBarcode,
           clienteLabel,
           userId: auth.userId,
           origin,
@@ -83,7 +82,6 @@ export async function POST(request: Request) {
     const jobId = await createBulkLabelJob({
       items: normalized.items,
       preset: normalized.preset,
-      includeBarcode: normalized.includeBarcode,
       clienteLabel: normalized.clienteLabel,
       userId: auth.userId,
       origin: requestOrigin(request),
@@ -122,7 +120,6 @@ export async function GET(request: Request) {
     items,
     preset: presetParsed.data,
     format: "pdf",
-    includeBarcode: false,
     clienteLabel: url.searchParams.get("clienteLabel") === "true",
   });
   if (!parsed.success) {
@@ -130,7 +127,7 @@ export async function GET(request: Request) {
   }
 
   const normalized = normalizeBulkLabelRequest(parsed.data);
-  const { preset, includeBarcode, clienteLabel, totalLabels } = normalized;
+  const { preset, clienteLabel, totalLabels } = normalized;
   if (!isBulkSyncCount(totalLabels)) {
     return NextResponse.json(
       { error: `Massimo ${BULK_SYNC_MAX} etichette per GET sync — usa POST bulk` },
@@ -145,7 +142,6 @@ export async function GET(request: Request) {
       await renderBulkLabelPdfSync({
         items: normalized.items,
         preset,
-        includeBarcode,
         clienteLabel,
         userId: auth.userId,
         origin,
