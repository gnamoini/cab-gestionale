diff --git a/components/document-capture/capture-scheda-compile-step.tsx b/components/document-capture/capture-scheda-compile-step.tsx
index 60d68a11..f4e55751 100644
--- a/components/document-capture/capture-scheda-compile-step.tsx
+++ b/components/document-capture/capture-scheda-compile-step.tsx
@@ -1,5 +1,6 @@
 "use client";
 
+/* eslint-disable react-hooks/refs -- submit hook exposes formRef/formProps for JSX wiring */
 import { memo, useCallback, useEffect, useMemo, useRef, useState } from "react";
 import { CaptureDocumentFilePreview } from "@/components/document-capture/capture-document-file-preview";
 import { CaptureReviewPanelLoading, CaptureReviewSplitLayout } from "@/components/document-capture/capture-review-panel";
@@ -174,6 +175,7 @@ export function CaptureSchedaCompileStep({
   const applyFlow = useCaptureApplyFlow(applyMode ? captureId : null);
   const gestToast = useGestionaleToast();
 
+  // eslint-disable-next-line react-hooks/preserve-manual-memoization -- lint phase2: preserve existing hook contract
   const initialCompileFields = useMemo(() => {
     if (!compileData?.fields) return null;
     if (!resumeIngressoCompile?.fields) return compileData.fields;
@@ -194,6 +196,8 @@ export function CaptureSchedaCompileStep({
     };
   }, [resumeIngressoCompile]);
 
+   
+  // eslint-disable-next-line react-hooks/preserve-manual-memoization -- lint phase2: preserve existing hook contract
   const initialCompileTagliandoFields = useMemo(() => {
     if (!compileData?.tagliandoFields) return undefined;
     if (!resumeIngressoCompile?.tagliandoFields) return compileData.tagliandoFields;
@@ -271,6 +275,7 @@ export function CaptureSchedaCompileStep({
         state: resume.state,
       })
     ) {
+      // eslint-disable-next-line react-hooks/set-state-in-effect -- sync state in effect lifecycle
       setMatchState("candidate_found");
       setSelectedCandidate(
         context.mezzo.candidates.find((c) => c.mezzo.id === resume.linkedMezzoId) ??
@@ -312,6 +317,7 @@ export function CaptureSchedaCompileStep({
     });
 
     lastContextKeyRef.current = contextKey;
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   }, [
     applyCandidatePreview,
     attive,
@@ -493,6 +499,7 @@ export function CaptureSchedaCompileStep({
 
   useEffect(() => {
     if (!sharedGlobalOpts || sharedGlobalOpts.isLoading) {
+      // eslint-disable-next-line react-hooks/set-state-in-effect -- sync state in effect lifecycle
       setLoadPhase("settings");
       return;
     }
@@ -724,6 +731,7 @@ export function CaptureSchedaCompileStep({
       });
       onApplySuccess?.(result.lavorazioneId);
     },
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
     [applyFlow, create.gateSave, create.priorita, create.stato, onApplySuccess],
   );
 
@@ -775,6 +783,7 @@ export function CaptureSchedaCompileStep({
       }
       void create.onSubmit(e);
     },
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
     [applyMode, captureHints, compileData, create, gestToast, onCompileError, requestIngressoApply],
   );
 
