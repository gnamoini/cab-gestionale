(async () => {
  await new Promise((r) => setTimeout(r, 6000));
   function runExtendedOverflowProbe() {   function cssSnapshot(el) {     const cs = getComputedStyle(el);     return {       width: cs.width,       minWidth: cs.minWidth,       maxWidth: cs.maxWidth,       display: cs.display,       position: cs.position,       flex: cs.flex,       grid: cs.gridTemplateColumns !== "none" ? cs.gridTemplateColumns : cs.grid,       overflow: cs.overflow,       overflowX: cs.overflowX,       transform: cs.transform,       flexWrap: cs.flexWrap,       flexGrow: cs.flexGrow,       flexShrink: cs.flexShrink,       flexBasis: cs.flexBasis,     };   }   function buildSelector(el) {     if (el.id) return "#" + el.id;     const tag = el.tagName.toLowerCase();     const cn =       typeof el.className === "string"         ? el.className.split(/\s+/).filter(Boolean).slice(0, 4).join(".")         : "";     return cn ? tag + "." + cn : tag;   }   function getReactHint(el) {     const key = Object.keys(el).find((k) => k.startsWith("__reactFiber"));     if (!key) return { component: null, file: null, lineApprox: null };     let fiber = el[key];     const chain = [];     for (let i = 0; i < 50 && fiber; i++) {       let name = null;       const t = fiber.type;       if (typeof t === "function") name = t.displayName || t.name;       else if (typeof t === "string") name = t;       if (         name &&         !["div", "span", "p", "a", "button", "input", "table", "tr", "td", "th", "Fragment"].includes(name)       ) {         const src = fiber._debugSource;         return {           component: name,           file: src?.fileName || null,           lineApprox: src?.lineNumber ?? null,           fiberChain: chain.slice(0, 8),         };       }       if (name) chain.push(name);       fiber = fiber.return;     }     return { component: null, file: null, lineApprox: null, fiberChain: chain.slice(0, 8) };   }   function promoteToRoot(hitEl, mainEl, innerWidth) {     const mainRight = mainEl.getBoundingClientRect().right;     let candidate = hitEl;     let cur = hitEl;     while (cur?.parentElement && mainEl.contains(cur.parentElement) && cur.parentElement !== mainEl) {       const p = cur.parentElement;       const pr = p.getBoundingClientRect();       if (pr.right > mainRight + 1 || pr.right > innerWidth + 1) {         candidate = p;         cur = p;       } else break;     }     return candidate;   }   const iw = window.innerWidth;   const doc = document.documentElement;   const body = document.body;   const shell = document.querySelector(".cab-app-shell");   const main = document.querySelector(".cab-app-shell main");   const pageContainer = main?.firstElementChild;   const chainEls = [     { name: "documentElement", el: doc },     { name: "body", el: body },     { name: "cab-app-shell", el: shell },     { name: "main", el: main },     { name: "pageContainer", el: pageContainer },   ];   const chain = chainEls     .filter((x) => x.el)     .map(({ name, el }) => {       const r = el.getBoundingClientRect();       const mainRight = main ? main.getBoundingClientRect().right : iw;       return {         name,         selector: buildSelector(el),         tag: el.tagName,         scrollWidth: el.scrollWidth,         clientWidth: el.clientWidth,         right: Math.round(r.right * 10) / 10,         overflowPx: Math.round(Math.max(0, r.right - iw) * 10) / 10,         mainClipPx: Math.round(Math.max(0, r.right - mainRight) * 10) / 10,         css: cssSnapshot(el),       };     });   const audit =     typeof window.__cabOverflowAudit === "function"       ? window.__cabOverflowAudit()       : { error: "__cabOverflowAudit not available" };   let worstLeaf = null;   let maxPx = 0;   if (main) {     const mainRight = main.getBoundingClientRect().right;     for (const el of main.querySelectorAll("*")) {       const cs = getComputedStyle(el);       if (cs.display === "none" || cs.visibility === "hidden") continue;       if (el.closest("[role=dialog], [aria-modal='true'], [data-cab-modal-root]")) continue;       const r = el.getBoundingClientRect();       const px = Math.max(Math.max(0, r.right - iw), Math.max(0, r.right - mainRight));       if (px > maxPx) {         maxPx = px;         worstLeaf = el;       }     }   }   let extendedRootCulprit = null;   if (worstLeaf && main) {     const rootEl = promoteToRoot(worstLeaf, main, iw);     const r = rootEl.getBoundingClientRect();     const mainRight = main.getBoundingClientRect().right;     extendedRootCulprit = {       selector: buildSelector(rootEl),       tag: rootEl.tagName,       className: typeof rootEl.className === "string" ? rootEl.className.slice(0, 240) : "",       rectRight: Math.round(r.right * 10) / 10,       width: Math.round(r.width * 10) / 10,       scrollWidth: rootEl.scrollWidth,       clientWidth: rootEl.clientWidth,       viewportOverflowPx: Math.round(Math.max(0, r.right - iw) * 10) / 10,       mainClipPx: Math.round(Math.max(0, r.right - mainRight) * 10) / 10,       css: cssSnapshot(rootEl),       react: getReactHint(rootEl),     };   }   const extras = [];   const seen = new Set();   for (const el of document.querySelectorAll("body > *, [data-cab-modal-root], .fixed, [role=dialog]")) {     if (!(el instanceof HTMLElement) || seen.has(el)) continue;     seen.add(el);     const cs = getComputedStyle(el);     const r = el.getBoundingClientRect();     const vpPx = Math.max(0, r.right - iw);     if (vpPx > 0 || cs.position === "fixed" || el.scrollWidth > el.clientWidth + 1) {       extras.push({         selector: buildSelector(el),         tag: el.tagName,         display: cs.display,         visibility: cs.visibility,         position: cs.position,         right: Math.round(r.right * 10) / 10,         overflowPx: Math.round(vpPx * 10) / 10,         scrollWidth: el.scrollWidth,         clientWidth: el.clientWidth,         inMain: main?.contains(el) ?? false,         css: cssSnapshot(el),         react: getReactHint(el),       });     }   }   const headerMirror = document.querySelector(".cab-gestionale-scroll-gutter-mirror");   let headerMirrorProbe = null;   if (headerMirror instanceof HTMLElement) {     const r = headerMirror.getBoundingClientRect();     headerMirrorProbe = {       selector: buildSelector(headerMirror),       scrollWidth: headerMirror.scrollWidth,       clientWidth: headerMirror.clientWidth,       right: r.right,       overflowPx: Math.max(0, r.right - iw),       css: cssSnapshot(headerMirror),       scrollbarInset: getComputedStyle(document.documentElement).getPropertyValue("--cab-main-scrollbar-inset"),     };   }   return {     pathname: location.pathname,     exportedAt: new Date().toISOString(),     innerWidth: iw,     shellTier: shell?.getAttribute("data-gestionale-shell-tier"),     document: {       scrollWidth: doc.scrollWidth,       clientWidth: doc.clientWidth,       hasOverflow: doc.scrollWidth > doc.clientWidth + 1,     },     body: {       scrollWidth: body.scrollWidth,       clientWidth: body.clientWidth,       hasOverflow: body.scrollWidth > body.clientWidth + 1,     },     chain,     audit,     extendedRootCulprit,     headerMirrorProbe,     portalsAndFixed: extras.slice(0, 40),   }; } 
  return runExtendedOverflowProbe();
})()