diff --git a/components/ordini-fornitori/ordini-fornitori-view.tsx b/components/ordini-fornitori/ordini-fornitori-view.tsx
index 442d7f01..5a41b55d 100644
--- a/components/ordini-fornitori/ordini-fornitori-view.tsx
+++ b/components/ordini-fornitori/ordini-fornitori-view.tsx
@@ -53,7 +53,7 @@ import { useClientPagination } from "@/lib/ui/use-client-pagination";
 import { useResponsiveListPageSize } from "@/lib/ui/use-responsive-list-page-size";
 import {
   dsBtnNeutral,
-  dsInput,
+
   dsLabel,
   dsPageToolbarCtaCompact,
   dsTableActionBtnDanger,
@@ -144,7 +144,6 @@ export function OrdiniFornitoriView({
   listSurface,
   canRead,
   canWrite,
-  initialOrdineId,
 }: {
   listSurface: ListSurface;
   /** Permessi pagina Preventivi (read = visualizza, write = modifica). */
@@ -162,8 +161,8 @@ export function OrdiniFornitoriView({
     searchInput,
     setSearchInput,
     searchApplied,
-    flushSearch: flushPageSearch,
-    clearSearch,
+    flushSearch: _flushPageSearch,
+    _clearSearch,
   } = useGestionaleListSearch({ domain: "ordini-fornitori" });
   const [filters, setFilters] = useState<OrdiniFornitoriPageFilters>(ORDINI_FORNITORI_FILTERS_EMPTY);
   const [filtriEspansi, setFiltriEspansi] = useCollapsiblePreference(
@@ -279,6 +278,7 @@ export function OrdiniFornitoriView({
       setEditor(null);
       await reload();
     },
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
     [reload, resetPage, upsertOrdineInListCache],
   );
 
@@ -375,7 +375,7 @@ export function OrdiniFornitoriView({
         <section aria-label="Azioni e filtri ordini fornitori">
           <PageToolbar
             primaryAction={
-              <div className="flex flex-wrap gap-2">
+              <div className="flex gap-2 min-w-0 flex-nowrap sm:flex-wrap">
                 <OptionalTooltip content={!canWrite ? READONLY_PERMISSION_HINT : undefined}>
                   <button
                     type="button"
