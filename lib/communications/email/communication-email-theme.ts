/** Token CAB per email HTML — allineati a `app/globals-core.css` (:root). */
export const COMM_EMAIL_THEME = {
  bgApp: "#f4f4f5",
  surface: "#ffffff",
  text: "#18181b",
  textMuted: "#71717a",
  border: "#e4e4e7",
  borderStrong: "#d4d4d8",
  primaryDefault: "#ff6633",
  shadowSm: "0 1px 2px rgb(0 0 0 / 0.05), 0 1px 3px rgb(0 0 0 / 0.08)",
  radiusLg: "12px",
  fontStack:
    "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif",
} as const;

export const COMM_EMAIL_LOGO_CID = "cab-email-logo" as const;
