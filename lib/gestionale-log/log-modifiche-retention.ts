/** Max voci `log_modifiche` per (entita, entita_id) — allineato a app_settings audit.retention. */
export { LOG_MODIFICHE_ENTITY_RETENTION_DEFAULT as LOG_MODIFICHE_RETENTION_PER_ENTITA } from "@/lib/audit/retention-config";
