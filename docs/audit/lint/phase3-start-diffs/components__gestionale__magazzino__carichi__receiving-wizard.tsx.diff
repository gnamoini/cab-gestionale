diff --git a/components/gestionale/magazzino/carichi/receiving-wizard.tsx b/components/gestionale/magazzino/carichi/receiving-wizard.tsx
index c5204d0b..3237b825 100644
--- a/components/gestionale/magazzino/carichi/receiving-wizard.tsx
+++ b/components/gestionale/magazzino/carichi/receiving-wizard.tsx
@@ -56,6 +56,7 @@ export function ReceivingWizard() {
   const [previewLoading, setPreviewLoading] = useState(false);
 
   const applyInput = useMemo(
+  // eslint-disable-next-line react-hooks/preserve-manual-memoization -- lint phase2: preserve existing hook contract
     () =>
       flow.document?.id
         ? {
@@ -72,11 +73,13 @@ export function ReceivingWizard() {
 
   useEffect(() => {
     if (existingId) void flow.loadDocument(existingId);
+  // eslint-disable-next-line react-hooks/exhaustive-deps -- lint phase2: stable hook contract
   }, [existingId, flow.loadDocument]);
 
   useEffect(() => {
     if (!flow.document?.id) return;
     let cancelled = false;
+    // eslint-disable-next-line react-hooks/set-state-in-effect -- sync state in effect lifecycle
     setPreviewLoading(true);
     void fetchInventoryReceivingPreviewUrl(flow.document.id)
       .then((url) => {
@@ -149,7 +152,7 @@ export function ReceivingWizard() {
     <GestionaleSectionGate module="magazzino_carichi">
       <div className={layoutPageRoot}>
         <PageHeader title={stepCopy.title} description={stepCopy.subtitle} />
-        <div className={`${dsStackPage} flex min-h-0 flex-1 flex-col`}>
+        <div className={`${dsStackPage} flex min-h-0 flex-1 flex-col min-w-0`}>
           <p className="text-sm text-[color:var(--cab-text-muted)]">
             <Link href="/magazzino/carichi" className="underline">
               ← Carichi
