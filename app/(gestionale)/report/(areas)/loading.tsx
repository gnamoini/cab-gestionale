import { ReportPageStructure } from "@/components/report/report-page-structure";

/** ponytail: PageLayout in page.tsx LEVEL 2 — skeleton area con toolbar. */
export default function ReportAreasLoading() {
  return <ReportPageStructure mode="skeleton" variant="area" />;
}
