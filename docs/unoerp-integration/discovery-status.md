# Discovery UnoERP — stato

**Stato: COMPLETED (READ-ONLY)** — 2026-09-03

Discovery eseguita su `casbari.unoerp.it`. Dettaglio: [`discovery-run.md`](discovery-run.md).

```text
npx tsx scripts/unoerp-discovery.ts
npx tsx scripts/unoerp-module-scan.ts      # scan esteso
npx tsx scripts/unoerp-detail-samples.ts   # campioni anonimizzati
```

**WRITE TESTS EXECUTED = 0**

Gate A: [`gate-a.md`](gate-a.md) — Preventivo BLOCKED, DDT PASS_CONDITIONED, Consuntivo BLOCKED.
